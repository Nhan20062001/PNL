// import requests from "@/config/axios";

export class ArticleService {
  static prefix = "articles";

  // static getAllArticles(params: object): Promise<any[]> {
  //   console.log(params);
  //   // return requests.get(`${this.prefix}`);
  //   return params;
  // }
}
